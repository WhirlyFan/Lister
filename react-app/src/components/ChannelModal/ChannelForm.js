import React, { useState } from "react";
import { useDispatch } from "react-redux";
import styles from "./ChannelForm.module.css";

export default function ChannelForm({
  hasClicked,
  setHasClicked,
  setShowModal,
}) {
  return <div>ChannelForm</div>;
}
